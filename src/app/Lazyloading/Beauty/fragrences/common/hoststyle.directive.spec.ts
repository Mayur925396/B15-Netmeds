import { HoststyleDirective } from './hoststyle.directive';

describe('HoststyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HoststyleDirective();
    expect(directive).toBeTruthy();
  });
});
